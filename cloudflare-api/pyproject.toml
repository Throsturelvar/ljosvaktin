[project]
name = "northseek-api"
version = "0.1.0"
description = "Northseek Cloudflare Python API"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk"
]
